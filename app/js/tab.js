function addSymbol(name) {
    var webv = document.getElementById(`webview-${focusedTab}`)
}

function removeSymbole(name) {
    var webv = document.getElementById(`webview-${focusedTab}`)
}

function clearSymboles() {
    var webv = document.getElementById(`webview-${focusedTab}`)
}